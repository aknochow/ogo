---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: openshellgateways.gateway.ogo.aknochow.io
spec:
  group: gateway.ogo.aknochow.io
  names:
    kind: OpenShellGateway
    listKind: OpenShellGatewayList
    plural: openshellgateways
    singular: openshellgateway
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.gatewayURL
      name: URL
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenShellGateway is a cluster-scoped singleton that manages an
          OpenShell Gateway instance.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenShellGatewaySpec defines the desired state of the OpenShell
              Gateway.
            properties:
              auth:
                description: Auth configures authentication.
                properties:
                  allowUnauthenticated:
                    default: false
                    description: AllowUnauthenticated enables unauthenticated access
                      (dev mode only, not recommended).
                    type: boolean
                  openshift:
                    description: |-
                      OpenShift configures authentication via OpenShift's built-in OAuth server.
                      This is the default and recommended auth method on OpenShift.
                      The operator deploys an auth-bridge sidecar that translates OpenShift
                      OAuth tokens to standard OIDC JWTs for the gateway.
                    properties:
                      adminGroup:
                        description: |-
                          AdminGroup is the OpenShift group that maps to the openshell-admin role.
                          Users in this group get admin access to the gateway.
                        type: string
                      enabled:
                        description: |-
                          Enabled activates OpenShift OAuth authentication.
                          Auto-detected on OpenShift when nil.
                        type: boolean
                    type: object
                type: object
              database:
                description: Database configures the PostgreSQL connection.
                properties:
                  secretName:
                    description: |-
                      SecretName references a Secret containing the PostgreSQL connection URI.
                      The Secret must have a key named "uri".
                    minLength: 1
                    type: string
                required:
                - secretName
                type: object
              image:
                default: ghcr.io/nvidia/openshell/gateway
                description: Image is the gateway container image.
                type: string
              imageTag:
                description: ImageTag overrides the default image tag.
                type: string
              logLevel:
                default: info
                description: LogLevel sets the gateway log level.
                enum:
                - trace
                - debug
                - info
                - warn
                - error
                type: string
              namespace:
                default: ogo
                description: Namespace where the gateway Deployment and associated
                  resources are created.
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([a-z0-9-]*[a-z0-9])?$
                type: string
              networkPolicy:
                description: NetworkPolicy controls whether a NetworkPolicy restricting
                  sandbox SSH is created.
                properties:
                  enabled:
                    default: true
                    description: Enabled creates a NetworkPolicy restricting SSH (port
                      2222) on sandbox pods to gateway pods only.
                    type: boolean
                type: object
              replicas:
                default: 1
                description: Replicas is the number of gateway pod replicas.
                format: int32
                minimum: 1
                type: integer
              resources:
                description: Resources defines resource requests and limits for gateway
                  pods.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              route:
                description: Route configures the OpenShift Route for external access.
                properties:
                  enabled:
                    description: Enabled creates an OpenShift Route. Auto-detected
                      on OpenShift when nil.
                    type: boolean
                  hostname:
                    description: Hostname is the custom hostname for the Route.
                    type: string
                type: object
              sandbox:
                description: Sandbox configures sandbox pod defaults.
                properties:
                  appArmorProfile:
                    default: Unconfined
                    description: AppArmorProfile for sandbox pods.
                    type: string
                  defaultImage:
                    default: quay.io/aap/carbonite:latest
                    description: DefaultImage is the default container image for sandbox
                      pods.
                    type: string
                  imagePullPolicy:
                    description: ImagePullPolicy for sandbox pod images.
                    enum:
                    - Always
                    - IfNotPresent
                    - Never
                    type: string
                  namespace:
                    description: Namespace where sandbox pods run. Defaults to the
                      gateway namespace.
                    type: string
                  runtimeClassName:
                    description: RuntimeClassName is an optional RuntimeClass for
                      sandbox pods (e.g. kata, gvisor).
                    type: string
                  workspaceStorageSize:
                    default: 2Gi
                    description: WorkspaceStorageSize is the PVC size for sandbox
                      workspace volumes.
                    type: string
                type: object
              supervisorImage:
                default: ghcr.io/nvidia/openshell/supervisor
                description: SupervisorImage is the supervisor container image sideloaded
                  into sandbox pods.
                type: string
              tls:
                description: TLS configures TLS and mTLS.
                properties:
                  certManager:
                    description: |-
                      CertManager configures cert-manager for server TLS certificates.
                      This is the recommended approach — uses your cluster's cert-manager
                      operator (e.g. with Let's Encrypt) for trusted server certificates.
                      Client mTLS certificates are always self-signed by the operator.
                    properties:
                      enabled:
                        description: Enabled uses cert-manager to issue server TLS
                          certificates.
                        type: boolean
                      issuerKind:
                        default: ClusterIssuer
                        description: IssuerKind is the kind of issuer (ClusterIssuer
                          or Issuer).
                        enum:
                        - ClusterIssuer
                        - Issuer
                        type: string
                      issuerName:
                        default: letsencrypt
                        description: IssuerName is the name of the cert-manager ClusterIssuer
                          or Issuer to use.
                        type: string
                    type: object
                  enabled:
                    default: true
                    description: Enabled controls whether TLS is enabled.
                    type: boolean
                  serverCertSecretName:
                    description: |-
                      ServerCertSecretName references a pre-existing kubernetes.io/tls Secret for the server.
                      When set, the operator will not generate or request server certificates.
                    type: string
                type: object
            required:
            - database
            type: object
          status:
            description: OpenShellGatewayStatus defines the observed state of the
              OpenShell Gateway.
            properties:
              clientCertSecretName:
                description: |-
                  ClientCertSecretName is the name of the Secret containing the client mTLS certificate.
                  CI systems can extract this to authenticate with the gateway.
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the gateway's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              gatewayURL:
                description: GatewayURL is the external URL for accessing the gateway.
                type: string
              observedGeneration:
                description: ObservedGeneration is the latest generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is the current lifecycle phase of the gateway.
                enum:
                - Pending
                - Creating
                - Running
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
