---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: openshellpolicies.gateway.ogo.aknochow.io
spec:
  group: gateway.ogo.aknochow.io
  names:
    kind: OpenShellPolicy
    listKind: OpenShellPolicyList
    plural: openshellpolicies
    singular: openshellpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.policyName
      name: Policy
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenShellPolicy defines a sandbox policy template for the OpenShell
          Gateway.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenShellPolicySpec defines a sandbox policy template for
              the OpenShell Gateway.
            properties:
              filesystem:
                description: Filesystem defines filesystem access policies.
                properties:
                  includeWorkdir:
                    description: IncludeWorkdir includes the sandbox working directory
                      in the filesystem policy.
                    type: boolean
                  readOnly:
                    description: ReadOnly paths accessible to the sandbox.
                    items:
                      type: string
                    type: array
                  readWrite:
                    description: ReadWrite paths accessible to the sandbox.
                    items:
                      type: string
                    type: array
                type: object
              network:
                additionalProperties:
                  description: NetworkPolicyRule defines a named set of network access
                    rules.
                  properties:
                    binaries:
                      description: Binaries defines which binaries can access these
                        endpoints.
                      items:
                        description: NetworkBinary defines an allowed binary for network
                          access.
                        properties:
                          path:
                            description: Path is the absolute path to the binary.
                            type: string
                        required:
                        - path
                        type: object
                      type: array
                    endpoints:
                      description: Endpoints defines allowed network endpoints.
                      items:
                        description: NetworkEndpoint defines an allowed network destination.
                        properties:
                          access:
                            description: Access is the access shorthand (read-only,
                              read-write, full).
                            enum:
                            - read-only
                            - read-write
                            - full
                            type: string
                          enforcement:
                            default: enforce
                            description: Enforcement is the enforcement mode (enforce
                              or audit).
                            enum:
                            - enforce
                            - audit
                            type: string
                          host:
                            description: Host is the hostname or glob pattern (e.g.
                              "*.example.com").
                            type: string
                          port:
                            description: Port is the destination port.
                            format: int32
                            type: integer
                          protocol:
                            description: Protocol is the application protocol (rest,
                              websocket, graphql, sql, or empty for L4).
                            enum:
                            - rest
                            - websocket
                            - graphql
                            - sql
                            - ""
                            type: string
                        required:
                        - host
                        - port
                        type: object
                      type: array
                    name:
                      description: Name is the display name for this network policy
                        rule.
                      type: string
                  required:
                  - name
                  type: object
                description: Network defines network access policies keyed by policy
                  name.
                type: object
              policyName:
                description: PolicyName is the name of the policy in the gateway.
                type: string
              process:
                description: Process defines process execution policies.
                properties:
                  runAsGroup:
                    description: RunAsGroup is the group name or GID to run sandbox
                      processes as.
                    type: string
                  runAsUser:
                    description: RunAsUser is the user name or UID to run sandbox
                      processes as.
                    type: string
                type: object
            required:
            - policyName
            type: object
          status:
            description: OpenShellPolicyStatus defines the observed state of the policy.
            properties:
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the latest generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is the current lifecycle phase.
                enum:
                - Pending
                - Synced
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
