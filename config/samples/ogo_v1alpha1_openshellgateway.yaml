apiVersion: gateway.ogo.aknochow.io/v1alpha1
kind: OpenShellGateway
metadata:
  labels:
    app.kubernetes.io/name: ogo
    app.kubernetes.io/managed-by: kustomize
  name: openshell
spec:
  namespace: ogo
  image: ghcr.io/nvidia/openshell/gateway
  replicas: 1
  database:
    secretName: openshell-pg-uri
  sandbox:
    defaultImage: ghcr.io/nvidia/openshell-community/sandboxes/base:latest
    workspaceStorageSize: "2Gi"
    appArmorProfile: "Unconfined"
  tls:
    enabled: true
  logLevel: info
  networkPolicy:
    enabled: true
